Saved to `./topics/es6-core-javascript-fundamentals.md`.

The document covers:

- **Scoping** (`var` vs `let`/`const`) and why it matters for data ownership
- **Arrow functions** and lexical `this` binding
- **Destructuring**, **spread/rest**, and **template literals** as data transformation tools
- **Classes** as syntax sugar over prototypes
- **ES6 modules** — static, file-scoped, tree-shakeable
- **Promises and async/await** — flattening async data flow
- **Array methods** (`map`, `filter`, `reduce`) as pure transformations
- 8 concrete code examples, 12 Q&A pairs, full vocabulary section
