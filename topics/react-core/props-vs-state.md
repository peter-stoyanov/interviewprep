Saved to `topics/props-vs-state.md`.

The document covers:

- **What they are**: props = data passed in (read-only), state = data owned internally (mutable)
- **The core rule**: data flows down via props, events flow up via callbacks
- **Key failure modes** without the distinction: silent mutation, stale copies, duplicate ownership
- **6 examples** from minimal to practical: including lifting state, derived data, and configuration vs behavior
- **11 Q&A pairs** for fast interview recall
- **Full vocabulary** grouped by nouns, verbs, and adjectives
