Saved to `topics/react-core/use-state.md`.

The document covers:

- **What it is** — a hook that gives function components a persistent, React-managed state slot
- **The core mechanic** — setter triggers re-render; state is a snapshot per render
- **Key pitfalls** — stale closures, mutating objects directly, using plain variables instead
- **6 examples** — toggle, functional updates, object state, stale closure bug + fix, controlled input, lazy initializer
- **11 Q&A pairs** for quick interview recall
- Full vocabulary with nouns, verbs, and adjectives
