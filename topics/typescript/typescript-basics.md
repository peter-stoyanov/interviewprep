Document saved to `topics/typescript/fundamentals.md`.

It covers:

- **Abstraction level**: language feature / type system layer — stays at the concept of static typing, not any specific framework usage
- **7 concrete examples** ranging from basic annotations to discriminated unions
- **12 quickfire Q&A** focused on common interview questions (`any` vs `unknown`, structural vs nominal, inference vs annotation)
- **Full vocabulary** grouped by nouns/verbs/adjectives with definitions tuned for interview precision
